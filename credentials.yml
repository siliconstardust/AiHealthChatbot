# Credentials for external channels
# Smart India Hackathon 2025 - Problem Statement 25049

# REST channel (for web integration)
rest:
# credentials.yml


twilio:
  account_sid: "${TWILIO_ACCOUNT_SID}"
  auth_token: "${TWILIO_AUTH_TOKEN}"
  twilio_number: "${TWILIO_NUMBER}"
  # No credentials needed

# Socket.IO channel (for real-time web chat)
socketio:
  user_message_evt: user_uttered
  bot_message_evt: bot_uttered
  session_persistence: true

# Twilio (for WhatsApp and SMS)
# Uncomment and add credentials when ready to use WhatsApp/SMS
# twilio:
#   account_sid: "YOUR_TWILIO_ACCOUNT_SID"
#   auth_token: "YOUR_TWILIO_AUTH_TOKEN"
#   twilio_number: "whatsapp:+14155238886"  # Twilio WhatsApp sandbox number
#   # For SMS, use regular phone number: "+1234567890"

# Telegram (alternative messaging platform)
# Uncomment and add your token when ready to use Telegram
# telegram:
#   access_token: "YOUR_TELEGRAM_BOT_TOKEN"
#   verify: "YOUR_TELEGRAM_BOT_NAME"
#   webhook_url: "https://your-domain.com/webhooks/telegram/webhook"

# Slack (for team collaboration)
# Uncomment when ready to use Slack
# slack:
#   slack_token: "YOUR_SLACK_BOT_TOKEN"
#   slack_channel: "YOUR_SLACK_CHANNEL"
#   slack_signing_secret: "YOUR_SLACK_SIGNING_SECRET"

# Facebook Messenger (alternative platform)
# Uncomment when ready to use Facebook
# facebook:
#   verify: "YOUR_VERIFY_TOKEN"
#   secret: "YOUR_APP_SECRET"
#   page_access_token: "YOUR_PAGE_ACCESS_TOKEN"

telegram:
  access_token: "YOUR_BOT_TOKEN"
  verify: "YOUR_BOT_USERNAME"
  webhook_url: "https://your-app.onrender.com/webhooks/telegram/webhook"

# Custom connector for Government SMS Gateway
# (To be configured with NIC SMS Gateway)
# Uncomment and configure when available
# government_sms:
#   api_key: "YOUR_NIC_API_KEY"
#   sender_id: "HEALTH"
#   api_url: "https://sms.gov.in/api/send"